data:
  dataset_dir: Dataset/binance_historical
  manifest_path: Dataset/binance_historical/manifest.json
  min_history_bars: 50000
  purge_gap_bars: 20
  seq_len: 96
  horizon: 20
  max_symbols: 34

training:
  seed: 42
  seq_len: 96
  preferred_backend: directml
  batch_size: 1024
  max_epochs: 50
  patience: 10
  lr: 0.001
  weight_decay: 0.0001
  num_workers: 0
  num_workers_non_cuda: 0
  compile_model: false
  checkpoint_root: models/checkpoints/trend_verify
  tensorboard_root: models/tensorboard/trend_verify

models:
  tcn:
    channels: 128
    dropout: 0.1

registry_path: model_registry.json